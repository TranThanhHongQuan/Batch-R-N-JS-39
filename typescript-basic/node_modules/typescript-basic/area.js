"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.area = void 0;
async function area(shape) {
    if (!shape.kind) {
        throw new Error("not valid shape");
    }
    switch (shape.kind) {
        case "circle":
            console.log("circle");
            return Math.PI * shape.radius ** 2;
        case "rectangle":
            console.log("rectangle");
            return shape.length * shape.width;
        case "square":
            console.log("square");
            return shape.side * shape.side;
        case "triangle":
            console.log("triangle");
            return 1 / 2 * shape.base * shape.height;
        case "parallelogram":
            console.log("Parallelogram");
            return shape.base * shape.height;
        default:
            throw new Error("Not valid shape to find a area");
    }
}
exports.area = area;
